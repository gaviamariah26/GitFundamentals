# Auto detect text files and perform LF normalization
* text=auto
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name = Mariah Gavia-Locke
	email = gaviamariah26@gmail.com